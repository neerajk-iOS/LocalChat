{
  "name": "localchat",
  "version": "2.0.0",
  "description": "LocalChat — self-hosted chat with voice/video calls",
  "private": true,
  "scripts": {
    "install:all": "cd server && npm install && cd ../client && npm install && cd ../mobile && npm install",
    "build": "cd client && npm run build",
    "start": "node server/index.js",
    "dev:server": "cd server && npx nodemon index.js",
    "dev:client": "cd client && npm run dev",
    "docker:build": "docker build -t localchat .",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "docker:rebuild": "docker compose down && docker build --no-cache -t localchat . && docker compose up -d"
  }
}
